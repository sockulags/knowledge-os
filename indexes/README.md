# Indexes

Generated catalogs are rebuildable caches. Run `kos index`; do not edit generated files manually. The catalog and SQLite FTS cache include discovery records and all discovery statuses for audit; context applies the separate discovery trust policy.
