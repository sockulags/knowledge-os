# Local source example

This sample contains the body-only retrieval term **xylophone**. It is intentionally outside managed durable roots so `kos ingest` can demonstrate provenance-preserving intake.
